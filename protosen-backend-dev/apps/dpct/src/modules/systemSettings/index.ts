import systemSettingsRouter from './systemSettings.routes';

/**
 * Module SystemSettings
 * Gère les paramètres système globaux (signature directeur, nom ministère, direction protocole)
 */
export default systemSettingsRouter;
